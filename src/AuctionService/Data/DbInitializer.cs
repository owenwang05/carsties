using System;

namespace AuctionService.Data;

public class DbInitializer
{
    // allows us to use it without initializing a new class
    public static void InitDB(WebApplication app)
    {
        
    }
}
